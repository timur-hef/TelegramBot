version: "3.9"

services:
  myapp:
    image: python:3.13
    working_dir: /app
    volumes:
      - .:/app
      - ./logs:/app/logs
    command: >
      bash -c "pip install -r requirements.txt && python bot.py"
    restart: always

